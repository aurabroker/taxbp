import InfoPage from "@/components/InfoPage";

export const runtime = "edge";
export const metadata = { title: "RODO — Beauty Polisa" };

export default function Page() {
  return <InfoPage title="RODO — nota informacyjna" />;
}
